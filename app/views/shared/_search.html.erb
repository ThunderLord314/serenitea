<div class="search">	
	<label for="search">Search:</label>
	<input type="text" id="search" placeholder="keyword"></input>
	<select>
		<% @categories.each do |cat| %>
			<option value="<%= cat.name %>"><%= cat.name %></option>
		<% end %>
	</select>
</div>
